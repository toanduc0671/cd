apiVersion: v1
kind: Service
metadata:
  labels:
    app: grafana
  name: grafana
  namespace: kubesphere-monitoring-system
spec:
  ports:
  - name: http
    port: 3000
    targetPort: http
    nodePort: {{ grafana_http_node_port }}
  selector:
    app: grafana
  type: NodePort
